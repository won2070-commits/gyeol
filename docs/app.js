import {axes,questions,extraQuestions,allQuestions,score,followups,quality,typeNames,typeProfiles,VERSION,validDraft} from './data.js';
const app=document.querySelector('#app');
const KEY='gyeol:state:v1';
let storageOK=true, page=0, extraIds=[], currentId=null, demo=false;
let state={draft:null,history:[]};
try{const raw=localStorage.getItem(KEY);if(raw){const p=JSON.parse(raw);state={draft:validDraft(p.draft)?p.draft:null,history:Array.isArray(p.history)?p.history.filter(h=>validDraft(h)&&typeof h.id==='string'&&Number.isFinite(h.date)&&questions.every(q=>h.answers[q.id])).slice(0,20):[]};}}catch{storageOK=false;}
if(state.draft){extraIds=Array.isArray(state.draft.extraIds)?state.draft.extraIds.filter(id=>extraQuestions.some(q=>q.id===id)):[];const maxPage=Math.ceil((extraIds.length||32)/4)-1;page=Number.isInteger(state.draft.page)?Math.max(0,Math.min(maxPage,state.draft.page)):0;}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const save=()=>{try{localStorage.setItem(KEY,JSON.stringify(state));storageOK=true;}catch{storageOK=false;announce('브라우저 저장을 사용할 수 없어 현재 화면에서만 유지됩니다. 결과를 파일로 저장해 주세요.');}};
const announce=t=>{document.querySelector('#status').textContent=t;};
const navItems=[['home','⌂','나의 공간'],['test','◎','성향 검사'],['history','◷','검사 기록'],['guide','▤','결의 분석 방식']];
function route(){return location.hash.slice(1)||'home';}
function go(to){if(route()===to)render();else location.hash=to;}
function shell(body){const r=route();app.innerHTML=`<aside class="sidebar"><a href="#home" class="brand" aria-label="결 홈"><span class="brand-mark">결</span><span>gyeol<span class="brand-sub">나를 알아가는 시간</span></span></a><nav aria-label="주 메뉴">${navItems.map(([key,icon,label])=>`<a href="#${key}" ${r===key?'aria-current="page"':''}><span aria-hidden="true">${icon}</span>${label}</a>`).join('')}</nav><div class="side-note"><span class="mini-flower" aria-hidden="true">✳</span><p>사람은 네 글자보다<br>훨씬 더 다채로우니까.</p><a href="#guide">결의 생각 읽기 <span aria-hidden="true">↗</span></a></div><div class="side-bottom">나의 속도로, 나답게.</div></aside><div class="workspace"><header class="topbar"><span>나를 이해하는 작은 발견</span><a href="#history" class="local-badge"><span aria-hidden="true">◉</span> 이 기기에만 저장</a></header><main id="main" tabindex="-1">${!storageOK?'<div class="warning" role="alert">브라우저 저장을 사용할 수 없어요. 페이지를 닫기 전에 결과를 파일로 저장해 주세요.</div>':''}${body}</main><footer><span>결 · 성격 탐색 도구</span><span>공식 MBTI® 검사가 아닙니다.</span><a href="#privacy">개인정보와 저장 안내</a></footer></div>`;bind();}
function flower(){return `<div class="flower-art" aria-hidden="true"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="petals"><i></i><i></i><i></i><i></i></div><div class="flower-core"></div><span class="art-note art-note-one">생각하는 나</span><span class="art-note art-note-two">느끼는 나</span><span class="art-note art-note-three">있는 그대로의 나</span></div>`;}
function home(){const count=Object.keys(state.draft?.answers||{}).filter(id=>id.startsWith('q')).length;return `<div class="page-title"><div><p class="eyebrow">MY PERSONALITY JOURNEY</p><h1>안녕하세요,<br class="mobile-only"> 오늘의 나는 어떤가요?</h1><p>정답을 찾기보다, 나를 조금 더 이해하는 시간.</p></div><span class="edition">나의 발견 노트 <span aria-hidden="true">↗</span></span></div><section class="hero"><div class="hero-copy"><span class="tag">나의 성향 알아보기</span><h2>네 글자 너머,<br>나의 <em>결</em>을 발견하다.</h2><p>일상 속 선택으로 살펴보는 나의 성향.<br>딱 잘라 말하기보다, 당신의 다양한 면을 함께 읽어요.</p><div class="hero-meta"><span>32문항 + 맞춤 추가 질문</span><span>약 5–8분 · 예상</span></div><button class="primary" data-action="start">${count?`이어서 검사하기 · ${count}/32`:'나의 성향 알아보기'} <span aria-hidden="true">↗</span></button><a href="#example" class="text-link">결과 먼저 둘러보기 <span aria-hidden="true">→</span></a></div>${flower()}</section><section class="promise-strip" aria-label="검사 특징"><span><b aria-hidden="true">✓</b> 가입 없이 시작</span><span><b aria-hidden="true">✓</b> 중간에 쉬어도 괜찮아요</span><span><b aria-hidden="true">✓</b> 내 답변은 내 기기에</span></section><section class="below-grid"><div class="journey"><div class="section-heading"><h2>이렇게 나를 알아가요</h2><span>세 번의 작은 발견</span></div><ol class="journey-list"><li><span class="step">01</span><div><h3>평소의 나를 떠올리기</h3><p>잘 보이고 싶은 모습보다, 최근 일상의 나로 답해요.</p></div></li><li><span class="step">02</span><div><h3>애매한 성향은 한 번 더</h3><p>양쪽 성향이 비슷하면 다른 상황의 질문을 더 살펴봐요.</p></div></li><li><span class="step">03</span><div><h3>내 경험과 함께 읽기</h3><p>네 가지 성향을 보고, 내게 맞는 설명인지 돌아봐요.</p></div></li></ol></div><aside class="insight"><span class="insight-label">결의 작은 생각</span><span class="insight-symbol" aria-hidden="true">“</span><h2>어제와 오늘의 답이<br>조금 달라도 괜찮아요.</h2><p>환경과 경험에 따라 나를 표현하는 방식은 달라질 수 있어요. 한 번의 결과로 나를 제한하지 마세요.</p><a href="#guide">결과를 더 잘 이해하는 방법 <span aria-hidden="true">↗</span></a></aside></section>`;}
function start(){demo=false;if(!state.draft){state.draft={version:VERSION,answers:{},started:Date.now()};save();}if(!extraIds.length){const missing=questions.findIndex(q=>!state.draft.answers[q.id]);page=missing<0?7:Math.floor(missing/4);}go('test');}
function test(){if(!state.draft)return `<section class="empty"><span class="empty-symbol">◎</span><h1>있는 그대로의 나로 시작해요.</h1><p>최근 몇 달의 평소 모습을 떠올려 주세요.<br>좋거나 나쁜 답은 없어요. 애매하면 ‘중간’을 골라도 괜찮아요.</p><p class="muted">32문항 · 필요 시 최대 8문항 추가 · 예상 5–8분</p><button class="primary" data-action="start">검사 시작하기 →</button></section>`;
const list=extraIds.length?extraQuestions.filter(q=>extraIds.includes(q.id)):questions;const items=list.slice(page*4,page*4+4);const answered=Object.keys(state.draft.answers).filter(id=>list.some(q=>q.id===id)).length;const isLast=(page+1)*4>=list.length;
return `<div class="test-top"><a href="#home" class="text-link">← 나의 공간</a><span>${extraIds.length?'추가 탐색':`기본 검사 ${page+1} / 8`}</span><button class="quiet" data-action="pause">저장하고 쉬기</button></div><div class="test-header"><p class="eyebrow">${extraIds.length?'한 번 더 살펴볼게요':'있는 그대로 답해 주세요'}</p><h1>${extraIds.length?'비슷한 두 성향, 조금 더 알아볼까요?':'평소의 나와 얼마나 비슷한가요?'}</h1><p>${extraIds.length?'양쪽 성향이 비슷했던 부분을 다른 상황에서 살펴봅니다.':'최근 몇 달의 내 모습을 기준으로, 가장 가까운 답을 골라 주세요.'}</p></div><div class="progress-label"><span>${extraIds.length?'추가 질문':'기본 질문'}</span><span>${answered} / ${list.length} 답변</span></div><progress value="${answered}" max="${list.length}" aria-label="답변 진행률"></progress><form id="quiz" novalidate><div class="questions">${items.map((q,i)=>`<fieldset id="field-${q.id}"><legend><span class="qnum">${String(page*4+i+1).padStart(2,'0')}</span>${q.text}</legend><div class="choices">${['전혀 아니다','아닌 편이다','중간','그런 편이다','매우 그렇다'].map((s,j)=>`<label class="choice"><input type="radio" name="${q.id}" value="${j+1}" ${state.draft.answers[q.id]===j+1?'checked':''}><span class="choice-circle" aria-hidden="true">${j+1}</span><span>${s}</span></label>`).join('')}</div><p class="field-error" id="error-${q.id}" hidden>가장 가까운 답을 하나 골라 주세요.</p></fieldset>`).join('')}</div><div class="quiz-actions"><button type="button" class="secondary" data-action="previous" ${page===0&&!extraIds.length?'disabled':''}>← 이전</button><span class="saved">${storageOK?'답변은 자동 저장돼요':'현재 화면에서만 유지돼요'}</span><button class="primary" type="submit">${isLast?extraIds.length?'결과 보기':'답변 확인하기':'다음 질문'} →</button></div></form>`;}
function finish(){const answers={...state.draft.answers};const entry={version:VERSION,id:crypto.randomUUID(),date:Date.now(),answers,reflection:{}};state.history.unshift(entry);state.history=state.history.slice(0,20);currentId=entry.id;state.draft=null;extraIds=[];save();go('result');}
function next(){const list=extraIds.length?extraQuestions.filter(q=>extraIds.includes(q.id)):questions;const missing=list.slice(page*4,page*4+4).filter(q=>!state.draft.answers[q.id]);if(missing.length){for(const q of missing){document.querySelector(`#error-${q.id}`).hidden=false;document.querySelectorAll(`[name="${q.id}"]`).forEach(el=>{el.setAttribute('aria-invalid','true');el.setAttribute('aria-describedby',`error-${q.id}`);});}document.querySelector(`[name="${missing[0].id}"]`).focus();announce('아직 답하지 않은 질문이 있어요.');return;}if((page+1)*4<list.length){page++;render();return;}if(!extraIds.length){const extra=followups(state.draft.answers);if(extra.length){extraIds=extra.map(q=>q.id);page=0;render();return;}}finish();}
function sample(){return {answers:Object.fromEntries(questions.map(q=>[q.id,q.direction===1?(q.axis===0?2:4):(q.axis===0?4:2)])),reflection:{},date:Date.now()};}
function current(){return demo?sample():state.history.find(h=>h.id===currentId)||state.history[0];}
function scales(result){return result.axes.map(a=>`<div class="scale"><div class="scale-top"><h3>${a.name}</h3><span class="${a.borderline?'borderline':'clear-label'}">${a.borderline?'양쪽이 비슷해요':`${a.letter===a.left?a.leftName:a.rightName} 쪽으로 기울어요`}</span></div><div class="scale-endpoints"><span><b>${a.left}</b> ${a.leftName}</span><span>${a.rightName} <b>${a.right}</b></span></div><div class="scale-track" role="img" aria-label="${a.leftName} 0, ${a.rightName} 100 중 성향 위치 ${a.position}점"><span class="scale-mid"></span><span class="scale-point" style="left:${a.position}%"></span></div><div class="scale-caption"><span>0</span><span>성향 위치 ${a.position} / 100</span><span>100</span></div><p>${a.borderline?'이번 답변에서는 두 방식이 함께 나타났어요. 상황에 따라 달라지는지 돌아보세요.':a.desc[a.mean>0?0:1]}</p></div>`).join('');}
function result(){const entry=current();if(!entry)return `<section class="empty"><h1>아직 검사 결과가 없어요.</h1><p>나의 성향을 탐색하면 이곳에서 결과를 볼 수 있어요.</p><button class="primary" data-action="start">검사 시작하기 →</button></section>`;const r=score(entry.answers),q=quality(entry.answers),weak=r.axes.filter(a=>a.borderline);return `<div class="result-top"><span class="eyebrow">${demo?'미리 보는 결과 · 예시 데이터':'MY PERSONALITY NOTES'}</span><span>${demo?'실제 검사 결과가 아니에요':new Date(entry.date).toLocaleDateString('ko-KR')}</span></div><section class="result-hero"><div><p>지금 내 답변에 담긴 성향</p><h1>${r.type}</h1><h2>${typeNames[r.type]||'한 가지로 정하지 않아도 괜찮아요'}</h2><p>${r.type.includes('X')?'X는 두 성향의 점수가 같아 방향을 정하지 않았다는 뜻이에요.':'유형 이름은 답변을 이해하기 위한 요약이에요. 나의 모든 모습을 뜻하지는 않아요.'}</p></div><div class="result-art" aria-hidden="true">${r.type.split('').map(c=>`<span>${c}</span>`).join('')}</div></section>${weak.length?`<div class="notice"><b>${weak.map(a=>a.key.split('').join('·')).join(', ')}는 양쪽 성향이 비슷해요.</b><p>작은 답변 차이로 글자가 바뀔 수 있어요. 아래의 성향 분포를 함께 읽어 주세요.</p></div>`:''}${q.inconsistent>=3||q.neutral>=16?`<div class="warning"><b>답변을 다시 돌아볼 만한 부분이 있어요.</b><p>${q.neutral>=16?'중립 답변이 많아 선호 방향을 구분하기 어려울 수 있어요.':'서로 다른 방향의 설명에 함께 동의하거나 반대한 답변이 여러 쌍 있어요. 상황에 따라 행동이 달라지는지 살펴보세요.'} 이는 오류나 정확도 판정이 아닙니다.</p></div>`:''}${typeProfiles[r.type]?`<section class="panel profile"><div class="profile-head"><p class="eyebrow">이 유형은 이런 결이에요</p><h2>${r.type} · ${typeNames[r.type]}</h2><p>${typeProfiles[r.type].summary}</p></div><div class="profile-cols"><div><h3>이런 점이 자주 나타나요</h3><ul>${typeProfiles[r.type].strengths.map(t=>`<li>${t}</li>`).join('')}</ul></div><div><h3>이렇게 해보면 좋아요</h3><ul>${typeProfiles[r.type].growth.map(t=>`<li>${t}</li>`).join('')}</ul></div><div><h3>편하게 느끼는 환경</h3><p>${typeProfiles[r.type].fit}</p></div></div><p class="small muted">이 설명은 답변에서 나타난 성향을 바탕으로 한 일반적인 서술이에요. 맞지 않는 부분은 아래에 기록해 주세요.</p></section>`:''}<div class="result-grid"><section class="panel"><h2>네 가지 성향의 분포</h2><p class="muted">점수는 답변의 위치이며, 확률이나 정확도가 아닙니다.</p>${scales(r)}</section><section class="result-aside"><div class="panel"><h2>이번 주, 나를 위한 작은 실험</h2>${r.axes.slice(0,2).map(a=>`<div class="practice"><span>${a.name}</span><p>${a.borderline?'두 방식이 편했던 상황을 각각 하나씩 기록해 보세요.':a.tip[a.mean>0?0:1]}</p></div>`).join('')}<p class="small muted">정해진 처방이 아닌, 직접 확인해 볼 제안이에요.</p></div><div class="panel reflection"><h2>내 경험과 맞나요?</h2><p>결과와 다른 내 모습도 남겨 주세요.<br>이 기록은 검사 점수를 바꾸지 않아요.</p>${demo?'<p class="muted">실제 검사를 마치면 자기 확인을 기록할 수 있어요.</p>':`<label for="fit">설명의 공감 정도</label><select id="fit"><option value="">선택해 주세요</option>${['대체로 맞아요','일부만 맞아요','잘 모르겠어요'].map(s=>`<option ${entry.reflection?.fit===s?'selected':''}>${s}</option>`).join('')}</select><label for="reflection">내가 발견한 모습</label><textarea id="reflection" maxlength="1000" rows="3" placeholder="예: 일할 때와 친구를 만날 때의 내가 달라요.">${esc(entry.reflection?.note||'')}</textarea><button class="secondary" data-action="reflect">나의 생각 저장</button><p id="reflection-status" class="small"></p>`}</div></section></div><div class="result-actions">${demo?'<button class="primary" data-action="start">내 성향 알아보기 →</button>':'<button class="primary" data-action="export">결과 파일로 저장 ↓</button><a class="secondary" href="#history">이전 기록과 비교</a>'}<a class="text-link" href="#guide">이 결과는 어떻게 계산되나요? →</a></div><p class="disclaimer">결은 독자적인 성격 탐색 도구로, 공식 MBTI® 검사가 아닙니다. 문항과 점수 기준은 아직 심리측정 연구로 검증되지 않았습니다.</p>`;}
function history(){const h=state.history;return `<div class="page-title"><div><p class="eyebrow">MY DISCOVERIES</p><h1>조금씩 쌓이는 나의 기록</h1><p>결과가 달라졌다면, 그날의 상황도 함께 돌아보세요.</p></div></div>${h.length?`<section class="panel history-list">${h.map(e=>{const r=score(e.answers);return `<button class="history-row" data-entry="${esc(e.id)}"><span class="history-type">${r.type}</span><span><b>${typeNames[r.type]||'양쪽 성향을 함께 탐색한 날'}</b><small>${new Date(e.date).toLocaleString('ko-KR')}</small></span><span aria-hidden="true">↗</span></button>`;}).join('')}</section>${h.length>1?`<section class="panel compare"><h2>최근 두 번의 기록 비교</h2><p class="muted">오른쪽 성향을 향한 위치가 얼마나 달라졌는지 살펴보세요.</p><div class="table-wrap"><table><caption>최근 결과와 직전 결과의 성향 위치, 0–100점</caption><thead><tr><th scope="col">성향</th><th scope="col">직전</th><th scope="col">최근</th><th scope="col">변화</th></tr></thead><tbody>${axes.map((a,i)=>{const prev=score(h[1].answers).axes[i].position,now=score(h[0].answers).axes[i].position;return `<tr><th scope="row">${a.left} ↔ ${a.right}</th><td>${prev}</td><td>${now}</td><td>${now-prev>0?'+':''}${now-prev}점</td></tr>`;}).join('')}</tbody></table></div><p class="small muted">추가 질문 수와 당시 상황이 다를 수 있어요. 점수 변화가 성격의 실제 변화를 증명하지는 않아요.</p></section>`:'<div class="notice">다음 검사를 마치면 두 기록의 성향을 나란히 비교할 수 있어요.</div>'}<div class="actions"><button class="primary" data-action="start">${state.draft?'진행 중 검사 이어하기':'새로 검사하기'} →</button><button class="quiet danger-text" data-action="delete">모든 기록 삭제</button></div><p class="small muted">이 브라우저에 최근 20회까지 보관합니다. 브라우저 데이터를 지우면 기록도 사라집니다.</p>`:`<section class="empty panel"><span class="empty-symbol" aria-hidden="true">◷</span><h2>아직 발견 노트가 비어 있어요.</h2><p>첫 검사를 마치면 나만의 기록이 여기에 쌓여요.</p><button class="primary" data-action="start">${state.draft?'이어서 검사하기':'첫 검사 시작하기'} →</button></section>`}`;}
function guide(){return `<div class="page-title"><div><p class="eyebrow">OUR APPROACH</p><h1>결과보다 중요한 건,<br>나를 이해하는 과정.</h1><p>결이 참고한 것, 계산하는 것, 아직 알 수 없는 것을 공개합니다.</p></div></div><section class="panel prose"><h2>좋은 경험에서 배우고, 문항은 새로 만들었어요.</h2><p>16Personalities의 이해하기 쉬운 성향 설명을 주된 참고로 삼고, MBTIonline의 자기 확인 과정과 Truity의 상세한 성향 설명 방식을 함께 살펴봤어요. ‘최고의 정확도’를 입증하는 독립 비교 연구를 확인한 것은 아닙니다.</p><div class="source-grid"><a href="https://www.16personalities.com/articles/our-theory" target="_blank" rel="noreferrer"><b>16Personalities ↗</b><span>쉬운 접근과 연속적인 성향 설명</span></a><a href="https://www.mbtionline.com/" target="_blank" rel="noreferrer"><b>MBTIonline ↗</b><span>공식 검사와 자기 이해 과정</span></a><a href="https://www.truity.com/test/type-finder-personality-test-new" target="_blank" rel="noreferrer"><b>Truity TypeFinder ↗</b><span>세부 성향에 대한 풍부한 설명</span></a></div><h2>질문과 점수는 이렇게 구성했어요.</h2><p>네 가지 성향마다 8문항, 총 32문항을 사용합니다. 각 성향에는 양쪽 방향의 질문이 4개씩 있어요. 질문 순서는 네 성향을 번갈아 보여줍니다.</p><p>답변 1–5를 −2부터 +2까지 바꾸고 문항의 방향을 반영해 평균을 구합니다. 이를 0–100의 성향 위치로 표시해요. 0은 왼쪽 성향, 100은 오른쪽 성향입니다. 평균이 정확히 중간이면 X로 표시합니다.</p><p>기본 답변의 정규화 평균이 −0.25부터 +0.25 사이이면 해당 성향에 2문항을 더 묻습니다. 최대 40문항이며, 추가 답변까지 같은 가중치로 계산합니다. ‘양쪽이 비슷함’ 기준도 같습니다. 이 기준은 제품 설계상의 임시 기준이며 통계적 신뢰구간이 아닙니다.</p><h2>‘정확하게 분석한다’고 말하려면 더 필요한 것</h2><p>현재 문항은 독자적으로 작성한 한국어 초안입니다. 실제 응답자 연구, 검사–재검사 신뢰도, 요인 구조, 한국어 문항 이해도 검증이 아직 없습니다. 추가 질문이나 역방향 문항이 있다는 사실만으로 정확도가 입증되지는 않습니다.</p><p>따라서 결은 스스로를 관찰하는 데 사용하세요. 특정 능력이나 직업 적합성, 관계의 성공을 판정하는 도구가 아닙니다. 16Personalities의 NERIS 모델도 공식 MBTI 검사와 동일하지 않습니다.</p><h2>답변 점검은 정답 검사와 달라요.</h2><p>반대 방향 문항 쌍에 강하게 같은 답을 하거나 중립 답변이 많으면 다시 돌아볼 안내를 보여줍니다. 이는 단순 점검 규칙이며 불성실 응답이나 정확도를 판정하지 않습니다.</p><h2>결과를 읽는 세 가지 방법</h2><ol><li>유형 이름보다 네 성향의 위치를 먼저 보세요.</li><li>설명과 맞지 않는 내 경험도 기록하세요.</li><li>다시 검사할 때는 비슷한 상황에서 답하고, 변화의 맥락을 살펴보세요.</li></ol><button class="primary" data-action="start">나의 성향 탐색하기 →</button></section>`;}
function privacy(){return `<section class="panel prose"><p class="eyebrow">YOUR DATA, YOUR SPACE</p><h1>내 기록은 어디에 있나요?</h1><h2>이 브라우저 안에만 있어요.</h2><p>검사 답변, 검사 시간, 자기 확인 메모는 브라우저의 로컬 저장소에 보관합니다. 서버로 전송하지 않으며, 로그인·추적 분석·외부 AI 호출을 사용하지 않습니다.</p><h2>공용 기기라면 사용 후 지워 주세요.</h2><p>같은 브라우저를 사용하는 다른 사람이 기록을 볼 수 있습니다. 아래 버튼으로 진행 중인 답변과 완료된 기록을 모두 삭제할 수 있어요.</p><button class="secondary danger-text" data-action="delete">이 브라우저의 모든 기록 삭제</button><h2>기기를 바꾸면 기록이 이어지지 않아요.</h2><p>브라우저 데이터 삭제나 시크릿 모드 종료로 기록이 사라질 수 있습니다. 결과 화면의 ‘결과 파일로 저장’으로 개인 보관용 JSON 파일을 내려받을 수 있어요. 현재 파일 가져오기는 제공하지 않습니다.</p><p>외부 참고 링크를 열면 해당 사이트의 개인정보 정책이 적용됩니다.</p><a class="text-link" href="#home">← 나의 공간으로</a></section>`;}
function render(){const r=route();if(r==='test'&&state.draft){state.draft.page=page;state.draft.extraIds=extraIds;save();}demo=r==='example';shell(({home,test,history,guide,privacy,result,example:result}[r]||home)());document.querySelector('main').focus({preventScroll:true});window.scrollTo({top:0,behavior:'instant'});}
function bind(){document.querySelector('.skip').addEventListener('click',e=>{e.preventDefault();document.querySelector('main').focus();});document.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>{const a=b.dataset.action;if(a==='start')start();if(a==='pause'){save();go('home');}if(a==='previous'){if(page>0)page--;else{extraIds=[];page=7;}render();}if(a==='delete'){const dialog=document.querySelector('#confirm');dialog.returnValue='';dialog.showModal();}if(a==='reflect'){const e=current();e.reflection={fit:document.querySelector('#fit').value,note:document.querySelector('#reflection').value};save();document.querySelector('#reflection-status').textContent=storageOK?'나의 생각을 저장했어요.':'현재 화면에만 저장됐어요. 결과를 파일로 보관해 주세요.';announce(document.querySelector('#reflection-status').textContent);}if(a==='export'){const e=current();const out={...e,result:score(e.answers),disclaimer:'공식 MBTI 검사가 아닌 미검증 성격 탐색 결과. 성향 위치는 정확도나 확률이 아닙니다.'};const url=URL.createObjectURL(new Blob([JSON.stringify(out,null,2)],{type:'application/json'}));const link=document.createElement('a');link.href=url;link.download=`gyeol-${new Date(e.date).toISOString().slice(0,10)}.json`;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);announce('결과 파일 저장을 시작했어요.');}}));
document.querySelectorAll('[data-entry]').forEach(b=>b.addEventListener('click',()=>{currentId=b.dataset.entry;go('result');}));
document.querySelector('#quiz')?.addEventListener('submit',e=>{e.preventDefault();next();});
document.querySelectorAll('input[type=radio]').forEach(input=>input.addEventListener('change',()=>{state.draft.answers[input.name]=Number(input.value);if(!extraIds.length){extraQuestions.forEach(q=>delete state.draft.answers[q.id]);}save();document.querySelector(`#error-${input.name}`).hidden=true;document.querySelectorAll(`[name="${input.name}"]`).forEach(el=>{el.removeAttribute('aria-invalid');el.removeAttribute('aria-describedby');});const list=extraIds.length?extraQuestions.filter(q=>extraIds.includes(q.id)):questions;const count=list.filter(q=>state.draft.answers[q.id]).length;document.querySelector('progress').value=count;document.querySelector('.progress-label span:last-child').textContent=`${count} / ${list.length} 답변`;document.querySelector('.saved').textContent=storageOK?'답변이 저장됐어요':'저장 불가 · 페이지를 닫지 마세요';}));}
document.querySelector('#confirm').addEventListener('close',e=>{if(e.target.returnValue==='delete'){state={draft:null,history:[]};currentId=null;page=0;extraIds=[];try{localStorage.removeItem(KEY);storageOK=true;}catch{storageOK=false;}go('history');announce('저장한 기록을 모두 삭제했어요.');}});
window.addEventListener('hashchange',render);render();
